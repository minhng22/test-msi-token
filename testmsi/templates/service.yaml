apiVersion: v1
kind: Service
metadata:
  name: {{ include "testmsi.fullname" . }}
  labels:
    {{- include "testmsi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "testmsi.selectorLabels" . | nindent 4 }}
